This tool uses Google Gemini 1.5 Flash to summarize epub novel.

## Usage

1. Set `GOOGLE_API_KEY` environment variable
2. Run `summarize <epub-file-path> -o <output>`

See `summarize -h` for more detail.
